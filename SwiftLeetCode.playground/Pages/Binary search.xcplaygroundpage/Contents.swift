
func makeBinarySearchIn(numbers: [Int], for number: Int) -> [Int] {
    var finish = false
    var startIndex = 0
    
    while !finish {
        for index in numbers.indices where index >= startIndex {
            
        }
    }
    
    return 0
}
